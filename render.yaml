services:
  - type: web
    name: joblens-api
    runtime: python
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false          # set manually in Render dashboard
      - key: DATABASE_URL
        value: sqlite:///./aijob.db
      - key: ALLOWED_ORIGINS
        sync: false          # e.g. https://your-app.vercel.app (or hostname only; API adds https://)
    disk:
      name: sqlite-data
      mountPath: /opt/render/project/src/backend
      sizeGB: 1
